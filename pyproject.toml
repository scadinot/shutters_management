[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
addopts = "-ra"

[tool.coverage.run]
source = ["custom_components.shutters_management"]
omit = ["tests/*"]

[tool.coverage.report]
show_missing = true
skip_covered = false
